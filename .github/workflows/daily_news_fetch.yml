# 工作流名称
name: Fetch Daily AI News (Chinese Only)

# 触发条件
on:
  workflow_dispatch:
  schedule:
    - cron: '0 7 * * *' # 每天 07:00 (UTC)

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    # 授予写入权限
    permissions:
      contents: write
    
    steps:
      # 步骤1：检出代码
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      # 步骤2：设置 Python 环境
      - name: Set up Python 🐍
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      # --- 关键修改：添加 'jieba' ---
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests PyYAML jieba # 添加 jieba 用于中文分词
      # --- 修改结束 ---

      # 步骤4：运行 Python 脚本
      - name: Run script to fetch news
        run: python ./_scripts/fetch_news.py
        env:
          NEWS_API_KEY: ${{ secrets.NEWS_API_KEY }}

      # 步骤5：提交更改
      - name: Commit and push if changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add _data/news.yml 
          
          if ! git diff --cached --quiet; then
            git commit -m "📰 [Bot] Update daily trend-based AI news"
            git push
          else
            echo "No changes to commit."
          fi
